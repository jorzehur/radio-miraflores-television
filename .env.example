# Base de datos (PostgreSQL requerido en Vercel; SQLite solo para desarrollo local)
# Local (SQLite):
DATABASE_URL="file:./db/custom.db"

# Producción (PostgreSQL - Neon o Supabase). Descomentar y usar la URL real:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"
